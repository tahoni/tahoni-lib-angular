import { SummaryOnErrorDirective } from './summary-on-error.directive';

describe('SummaryOnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new SummaryOnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
