import { TouchOnErrorDirective } from './touch-on-error.directive';

describe('TouchOnErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new TouchOnErrorDirective();
    expect(directive).toBeTruthy();
  });
});
